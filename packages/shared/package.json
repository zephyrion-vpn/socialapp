{
  "name": "@socialapp/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types, zod schemas, constants and utilities for the SocialApp backend and desktop client",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit --declaration false --declarationMap false"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
